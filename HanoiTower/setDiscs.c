#include "lib.h"

void setDiscs(tpilha *pilha,int numDiscs){
	int i=0, test=0;
	
	for(i=1;i<=numDiscs;i++){
		push(&(*pilha),i);
		/******************************************************************
		* chama a função push() e empilha a quantidade de discos passada *
		******************************************************************/	
	}		
}
